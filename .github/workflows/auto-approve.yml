name: Auto approve

on: pull_request

jobs:
  auto-approve:
    runs-on: ubuntu-latest
    steps:
      - uses: hmarr/auto-approve-action
        with:
          github-token: ${{ secrets.AUTO_APPROVE_TOKEN }}
